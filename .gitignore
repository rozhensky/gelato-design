# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Dependencies / build (if a tooling step is added later)
node_modules/
dist/
.cache/

# Env / secrets
.env
.env.local

# Local Claude Code settings (personal permissions, machine paths)
.claude/settings.local.json

# Founder original photo — keep private, site uses founder-portrait.png
assets/img/founder.png
